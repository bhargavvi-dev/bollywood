<%= form_for([:admin, @data_item, @content_item]) do |f| %>
<% if @content_item.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@content_item.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @content_item.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <table class="table table-hover">

 <%= f.fields_for @newsmetadata do |d| %>
       <tr>
       <td><%= d.label :language_code %></td>
       <td><%= d.select :language_code, ["en","ru"] ,required: true%></td>
      </tr>
      <%end%>
    
      <tr>
        <td><%= f.label :Title %></td>
        <td><%= f.text_field :title,required: true %></td>
      </tr>

      <tr>
        <td><%= f.label :description %></td>
        <td><%= f.text_area :description,required: true %></td>
      </tr>

      <tr>
         <td rowspan="2" align="center" ><%= f.submit %></td>
    </tr>
   </table>

<%end%>